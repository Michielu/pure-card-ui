import React from 'react';
import { Button } from 'react-bootstrap';

const AddButton = () => (
  <Button href="/add" className="pc-width-80 pc-margin-10">Add card</Button>
);

export default AddButton;
