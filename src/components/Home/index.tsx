import React from 'react';
// import { Button } from 'react-bootstrap';

import { addToStore, getFromStore, getAllFromStore } from '../../utils/IDB';

function Home() {
    return (
        <div>
            Hom
            {/* <button onClick={() => addToStore({ key: "key1", value: ["a", "1"] })}>Post to store</button>
            <button onClick={() => console.log("Get store", getFromStore("key1"))}>Get from store</button> */}

        </div >
    );
}

export default Home;
